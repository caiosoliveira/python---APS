mkdir projeto_elevenlabs && cd projeto_elevenlabs
python -m venv .venv
# Windows:
.venv\Scripts\activate


No VSCode, instala estas extensões:
- **Python** (Microsoft) — obrigatória
- **Jupyter** (Microsoft) — para os notebooks
- **Pylance** — autocomplete inteligente
- **Python Indent** — indentação automática

**0.2 — Instalar dependências**

Cria o ficheiro `requirements.txt`:
```
elevenlabs          # SDK oficial ElevenLabs (STT + TTS + clonagem)
sounddevice         # captura microfone em tempo real
scipy               # gravar/ler .wav
numpy               # manipulação de arrays de áudio
sentence-transformers  # embeddings semânticos para o bloco 3
pygame              # reproduzir o áudio TTS
python-dotenv       # gerir a API key sem a hardcodar
streamlit           # interface web para demo final
jupyter             # notebooks
ipykernel           # ligar o kernel ao VSCode

pip install -r requirements.txt
```

**0.3 — Estrutura de pastas**
```
projeto_elevenlabs/
├── .env                  # API key (nunca fazer commit disto!)
├── .gitignore
├── requirements.txt
├── main.py               # pipeline de linha de comandos
├── app.py                # interface Streamlit
├── audio_input.py
├── stt.py
├── response_engine.py
├── tts.py
├── voice_clone.py
├── responses.json
├── samples/              # áudios de teste e gravações para clonagem
└── notebooks/
    ├── 01_exploracao_api.ipynb
    ├── 02_audio_input.ipynb
    ├── 03_response_engine.ipynb
    └── 04_pipeline_completo.ipynb
```

**0.4 — Ficheiro `.env` e `.gitignore`**

`.env`:
```
ELEVENLABS_API_KEY=sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

`.gitignore`:
```
.env
.venv/
__pycache__/
*.pyc
samples/*.wav
samples/*.mp3



----


# Criar pastas
mkdir samples, notebooks

# Criar ficheiros Python vazios
New-Item audio_input.py, stt.py, response_engine.py, tts.py, voice_clone.py, main.py, app.py -ItemType File

# Criar responses.json vazio
New-Item responses.json -ItemType File
```

Ficas com a estrutura completa mas sem código — cada etapa preenche o ficheiro correspondente. Assim não te perdes a tentar perceber código de módulos que ainda não estudaste.

---

**Sobre a sequência** — sim, exactamente. A ordem é:
```
Estrutura de pastas ✓  →  Etapa 1 (API key + créditos)  →  Etapa 2 (audio_input.py)  →  ...
```

Confirma que o teu `projeto_elevenlabs` tem este aspecto antes de avançar:
```
projeto_elevenlabs/
├── .venv/
├── .env                ✓
├── .gitignore          ✓
├── requirements.txt    ✓
├── audio_input.py      (vazio)
├── stt.py              (vazio)
├── response_engine.py  (vazio)
├── tts.py              (vazio)
├── voice_clone.py      (vazio)
├── main.py             (vazio)
├── app.py              (vazio)
├── responses.json      (vazio)
├── samples/            (pasta vazia)
└── notebooks/          (pasta vazia)





New-Item 01_exploracao_api.ipynb, 02_audio_input.ipynb, 03_response_engine.ipynb, 04_pipeline_completo.ipynb -ItemType File